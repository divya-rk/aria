apiVersion: v1
kind: ServiceAccount
metadata:
  name: aria-worker
  namespace: aria
  labels:
    app.kubernetes.io/name: aria
    app.kubernetes.io/component: worker
  annotations:
    # IRSA annotation - will be patched per environment
    eks.amazonaws.com/role-arn: "${WORKER_ROLE_ARN}"
